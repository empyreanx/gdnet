/** 
 @file  utility.h
 @brief PENet utility header
*/
#ifndef __PENET_UTILITY_H__
#define __PENET_UTILITY_H__

#define PENET_MAX(x, y) ((x) > (y) ? (x) : (y))
#define PENET_MIN(x, y) ((x) < (y) ? (x) : (y))

#endif /* __PENET_UTILITY_H__ */
